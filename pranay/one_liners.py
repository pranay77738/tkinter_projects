# swap two variables

a,b=b,a

# take space separate inputs

L=list(map(int,input().split()))

# Factorial of a Number

import functools as p
R = p.reduce(lambda x,y: x*y, range(1,n+1))

# list comprehensions

squars = [x*x for x in range(11)]

#Fibonacci Series

lambda x: x if x<=1 else fib(x-1) + fib(x-2)

